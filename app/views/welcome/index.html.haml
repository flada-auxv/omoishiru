%h1 重い知る
%div
  %a{href: '/auth/twitter'} Sign in with Twitter
  %span
    Twitter ID:
    = current_user.twitter.name if current_user.try(:twitter)
%div
  %a{href: '/auth/withings'} Sign in with Withings
  %span
    Withings ID:
    = current_user.withings.name if current_user.try(:withings)

= link_to 'テスト', withings_notifications_path
