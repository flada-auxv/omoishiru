%h1 重い知る
%div
  = link_to_unless !!current_user.try(:twitter), 'Sign in with Twitter', '/auth/twitter'
  %span
    Twitter ID:
    = current_user.twitter.name if current_user.try(:twitter)
%div
  = link_to_unless !!current_user.try(:withings), 'Sign in with Withings', '/auth/withings'
  %span
    Withings ID:
    = current_user.withings.name if current_user.try(:withings)

= link_to 'Withings で測定した体重で Twitter の name を更新する', withings_subscribe_path
